version: 2
jobs:
  build:
    docker:
      - image: felicianotech/docker-hugo:latest
    steps:
      - checkout
      - run: git submodule sync
      - run: git submodule update --init --recursive
      - run: git clone git@github.com:taikii/taikii.github.io.git public
      - run: hugo
      #- run:
      #    name: "Test Website"
      #    command: htmlproofer ~/project/public --allow-hash-href --check-html --empty-alt-ignore
      - deploy:
          command: cd public && git add . && git diff --cached --exit-code --quiet || git commit -m "Rebuilding site" && git push origin master
